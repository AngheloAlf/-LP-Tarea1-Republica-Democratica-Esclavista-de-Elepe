#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#define LARGO 256

#include "stdlib.h"
#include "stdio.h"
#include "string.h"

int primerNumero(void);
char *leerComando(void);
char *agregarDB(char *palabra);
int charAInt(char *numero);

#endif